{
  "name": "other-service-client",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/other-service-client/src",
  "tags": ["scope:bff", "type:data-access"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @nx-serverless-bff/other-service-client build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @nx-serverless-bff/other-service-client lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @nx-serverless-bff/other-service-client test"
      }
    }
  }
}
